import { PlatformReviewHub } from "@/components/platform/platform-review-hub";

export const metadata = { title: "Release review — Platform" };

export default function PlatformReviewPage() {
  return <PlatformReviewHub />;
}
