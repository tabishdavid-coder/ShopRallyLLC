import { CrmInventoryTour } from "@/components/design-review/crm-inventory";

export const metadata = {
  title: "CRM inventory — ShopRally",
  description: "Tour every ShopRally CRM shell, port, and start command in suggested viewing order.",
};

export default function CrmInventoryPage() {
  return <CrmInventoryTour />;
}
