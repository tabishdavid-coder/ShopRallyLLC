"use client";

/**
 * Customer profile drawer — shared shell used from the customers list
 * and (via EstimateLabContextDrawer) the estimate workspace.
 */
export {
  EstimateLabContextDrawer as CustomerContextDrawer,
  type ContextDrawerTab,
  type CustomerDrawerSource,
} from "@/components/estimate-building/estimate-lab-context-drawer";
